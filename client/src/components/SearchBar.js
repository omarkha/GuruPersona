import React from "react";

const SearchBar = () => {
  return (
    <div className="searchbar">
      <input type="text" />
      <button>Search</button>
    </div>
  );
};

export default SearchBar;
